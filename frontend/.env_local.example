# Village/local frontend profile.
# Create frontend/.env_local, then select it with:
#   cp frontend/.env_local frontend/.env.local

VITE_API_URL=/api
VITE_APP_MODE=operational_pos
VITE_POS_DEPLOYMENT_PROFILE=offline
VITE_CUSTOMER_RETENTION_ENABLED=false
VITE_OFFLINE_QUEUE_ENABLED=false
